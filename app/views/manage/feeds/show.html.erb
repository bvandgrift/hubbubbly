<h1><%= @feed.name -%></h1>

<p>Url: <%= @feed.url -%></p>

<h2>Entries</h2>

<%= render :partial => 'manage/feed_items/feed_item', :collection => @feed.feed_items %>

<%#= render :partial => 'entry', :collection => @feed.entries %>